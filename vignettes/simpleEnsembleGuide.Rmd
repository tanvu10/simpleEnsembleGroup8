---
title: "simpleEnsembleGuide"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{simpleEnsembleGuide}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(simpleEnsembleGroup8)
```

```{r}
data(airquality)
clean_airquality <- na.omit(airquality)
model2 <- fit_linear_model(clean_airquality$Solar.R, clean_airquality[, -1], add_intercept = TRUE, bagging = FALSE)
```

